<li>
	<% if !@top5 %><%= gravatar_for user %> <% end %>
  <%= link_to user.name, user %>
  | <%= user.score%> puntos 
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "Eliminar", user, method: :delete,
                                  data: { confirm: "Seguro?" } %>
  <% end %>
</li>